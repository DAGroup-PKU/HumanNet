// Constants shared between the server and the frontend (mirrors of
// `src/lib/config-types.ts`). Kept tiny on purpose so we don't pull
// React-aware modules into Node.

export const ACCENT_OPEN = "⟦";
export const ACCENT_CLOSE = "⟧";

export const CONFIG_SCHEMA_VERSION = 1;
